
-- -----------------------------------------------------
-- Table `macdevice_DEVICE`
-- -----------------------------------------------------
CREATE  TABLE IF NOT EXISTS macdevice_DEVICE (
  macdevice_DEVICE_ID VARCHAR(45) NOT NULL ,
  DEVICE_NAME VARCHAR(100) NULL DEFAULT NULL,
  PRIMARY KEY (macdevice_DEVICE_ID) );

